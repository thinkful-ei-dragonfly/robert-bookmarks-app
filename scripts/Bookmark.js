//

'use strict';

/* individual bookmark module */
(function Bookmark() {

  // creates and returns a bookmark with the matching ID
  createBookmark( id, title, rating, link, description, isDisplayDetailed ) {
    this.id = id;
    this.title = id;
    this.rating = id;
    this.description = id;
    this.isDisplayDetailed = id;
    this.id = id;
  }

  id
  DataTransferItemListrating
  link
  RTCSessionDescription





  return {
    getId,
    
  }


})();

